using System;

namespace MiNET.Events.Player
{
    /*[Obsolete("Not currrently implemented")]
    public class PlayerInputEvent : PlayerEvent
    {
        public PlayerInput Input { get; }
        public PlayerInputState State { get; }
        public PlayerInputEvent(MiNET.Player player, PlayerInput input, PlayerInputState state) : base(player)
        {
            Input = input;
            State = state;
        }
    }*/
}
